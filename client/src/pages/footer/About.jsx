import { Typography } from '@mui/material';
import { LeftContent, RightContent, View } from '../../styles/auth';

export default function About() {
  return (
    <View>
      <LeftContent>
      Sobre AYNI
      
      </LeftContent>
      <RightContent>
      <Typography> Ayni es una palabra quechua que hace referencia a la reciprocidad o mutualismo.
      </Typography>
      </RightContent>
    </View>
  )
}
